import type { Metadata } from "next";

export const metadata: Metadata = {
	title: "Input",
	description:
		"Input compound component — Label, Slot, LeadingIcon, Field, Message. Figma DDV Form (node 73:6625).",
};

export default function Layout({ children }: { children: React.ReactNode }) {
	return children;
}
